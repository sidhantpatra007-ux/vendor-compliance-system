export type Severity = 'critical' | 'high' | 'medium' | 'low';
export interface Vendor { vendor_id:number; name:string; company_number:string; latest_score:number|null; latest_grade:string|null; last_checked:string|null; recommend_manual_review:boolean; data_confidence:string|null; }
export interface Alert { alert_id:number; vendor_id:number; severity:Severity; status:string; title:string; reason:string; evidence?: unknown[]; assigned_to:string|null; acknowledged_at?:string|null; resolved_at?:string|null; sla_due_at:string; created_at:string; }
export interface AuditEvent { id:number; action:string; actor:string|null; vendor_id?:number|null; alert_id?:number|null; details:Record<string,unknown>; created_at:string; }
export interface Evidence { evidence_id:number; concept:string; state:string; extraction_method:string; page:number; captured_at:string; image_url:string; companies_house_filing_history_url:string; }
export interface Decision { decision_id:number; decision:string; reviewer:string; note:string|null; next_review_at:string|null; created_at:string; }
export interface Detail { vendor_id:number; vendor_name:string; company_number:string; intake:Record<string, string|boolean|null>; latest:{ composite_score:number; risk_grade:string; checked_at:string; signals:Record<string,unknown>; categories:Record<string,{score:number;unscored?:boolean}>; factors:Array<{description:string;severity:string;points:number}>; recommend_manual_review:boolean; data_quality?:{confidence?:string;issues?:string[]} }; score_history:Array<{snapshot_id:number;checked_at:string;score:number;grade:string}>; financial_evidence:Evidence[]; review_decisions:Decision[]; alerts:Alert[]; audit_events:AuditEvent[]; }
