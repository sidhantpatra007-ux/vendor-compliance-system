import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom'; import { useAuth } from './hooks'; import { Layout } from './Layout'; import { Login, Overview, Vendors, VendorDetail, Alerts, Audit } from './pages';
const client=new QueryClient({defaultOptions:{queries:{retry:1,refetchOnWindowFocus:true}}});
function Gate({children}:{children:React.ReactNode}){const q=useAuth();if(q.isLoading)return <div className="grid min-h-screen place-items-center text-slate-400">Opening workspace…</div>;return q.data?.authenticated?<>{children}</>:<Navigate to="/login" replace/>}
export function App(){return <QueryClientProvider client={client}><BrowserRouter><Routes><Route path="/login" element={<Login/>}/><Route path="/" element={<Gate><Layout/></Gate>}><Route index element={<Overview/>}/><Route path="vendors" element={<Vendors/>}/><Route path="vendors/:id" element={<VendorDetail/>}/><Route path="alerts" element={<Alerts/>}/><Route path="audit" element={<Audit/>}/></Route></Routes></BrowserRouter></QueryClientProvider>}
